{{#section 'title'}}Product: {{data.name}}{{/section}}
{{#section 'current'}}products{{/section}}


<div class="mt-4">
  <div class="row">
    <div class="col-sm-0 col-lg-3">

    </div>
    <div class="col-sm-12 col-lg-9">
      <h2>{{data.name}}</h2>
      <img src="/uploads/products/{{data.img}}" class="img-thumbnail" alt="{{data.name}}">
      <br>
      <p>{{data.desc}}</p>
    </div>
  </div>
</div>