
<div class="mt-4">
  <div class="row">
    <div class="col-md-3 d-none d-md-block">
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/courses">Back to Courses</a>
          <a class="nav-link active" aria-current="page" href="/courses{{routeI 'show' data._id}}">Back to detail</a>
        </li>
      </ul>
    </div>
    <div class="col-md-9 col-sm-12">
      <h2 class="mb-4">Edit course: {{data.name}}</h2>
      <form action="/courses{{routeI 'update' data._id}}?_method=PUT" method="post" class="row g-3">
        <div class="col-md-4 col-sm-12">
          <label for="id" class="form-label">Id</label>
          <input type="text" class="form-control" id="id" placeholder="" value="{{data._id}}" readonly disabled>
        </div>
        <div class="col-md-4 col-sm-6">
          <label for="createdAt" class="form-label">Created at</label>
          <input type="text" class="form-control" id="createdAt" value="{{data.createdAt}}" readonly disabled>
        </div>
        <div class="col-md-4 col-sm-6">
          <label for="updatedAt" class="form-label">Updated at</label>
          <input type="text" class="form-control" id="updatedAt" value="{{data.updatedAt}}" readonly disabled>
        </div>
        <div class="col-md-6 col-sm-12">
          <label for="name" class="form-label">Name</label>
          <input type="text" class="form-control" id="name" name="name" placeholder="" value="{{data.name}}">
        </div>
        <div class="col-md-6 col-sm-12">
          <label for="slug" class="form-label">Slug</label>
          <input type="text" class="form-control" id="slug" placeholder="" value="{{data.slug}}" readonly disabled>
        </div>
        <div class="col-12">
          <label for="desc" class="form-label">Description</label>
          <textarea class="form-control" id="desc" name="desc" rows="5">{{data.desc}}</textarea>
        </div>
        <div class="col-12">
          <label for="img" class="form-label">Image</label>
          <input type="text" class="form-control" id="img" name="img" placeholder="" value="{{data.img}}">
        </div>
        <div class="col-12 mt-5 d-flex justify-content-end gap-2">
          <button type="reset" class="btn btn-secondary">Cancel</button>
          <button type="submit" class="btn btn-success">Update</button>
        </div>
      </form>
    </div>
  </div>
</div>