<div class="mt-4">
  <div class="row">
    <div class="col-sm-0 col-lg-4">

    </div>
    <div class="col-sm-12 col-lg-8">
      <h2>{{course.name}}</h2>
      <img src="/img/courses/{{course.img}}" class="img-thumbnail" alt="{{course.name}}">
      <br>
      <p>{{course.desc}}</p>
    </div>
  </div>
</div>