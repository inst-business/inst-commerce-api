{
  "name": "javascript",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "build:sass": "node-sass src/resources/scss/ --output src/public/css/ --no-source-map",
    "watch:sass": "yarn run build:sass && node-sass -wr src/resources/scss/ -o src/public/css/",
    "watch": "npm-run-all --parallel watch:*",
    "serve": "nodemon --inspect src/index.js",
    "dev": "npm-run-all --parallel watch serve",
    "start": "npm-run-all --parallel serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-handlebars": "^7.0.7",
    "json-serve": "^0.1.0",
    "json-server": "^0.17.3",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/btoann/api.instances.git"
  },
  "keywords": [],
  "bugs": {
    "url": "https://github.com/btoann/api.instances/issues"
  },
  "homepage": "https://github.com/btoann/api.instances#readme",
  "description": ""
}
